import random

# Dummy ML logic for plant health prediction
def analyze_plant_health(image_bytes):
    # Placeholder: Replace with real model inference later
    possible_results = ["Healthy", "Moderate", "Unhealthy"]
    return random.choice(possible_results)

